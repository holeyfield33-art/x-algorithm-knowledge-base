# Sources

Optional place to keep local extracts or mirrors of key upstream files for offline / reproducible RAG builds.

Do not commit large binary artifacts or full copies of the upstream repository unless necessary. Prefer referencing the original paths and commit SHAs.
